// including the std io header files
#include <stdio.h>

// the main entry point
int main() { // begin
	int sum = 0; // sum up the numbers
	for (int i = 0; i < 10; i += 1) // basic for-loop
		sum += i; // numbers will be added against the variable sum
	
	return 0;
} // end
